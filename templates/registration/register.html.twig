{% extends 'baseForm.html.twig' %}

{% block titleForm %}{{ parent() }} Inscription{% endblock %}

{% block bodyForm %}
    <h2 id="creeCompte">Créer un compte</h2>

    {{ form_start(registrationForm) }}
    {{ form_row(registrationForm.nom) }}
    {{ form_row(registrationForm.prenom) }}
    <div class="nature">
        {{ form_row(registrationForm.nature) }}
    </div>
    {{ form_row(registrationForm.email) }}
    {{ form_row(registrationForm.plainPassword, {
        label: 'Mot de passe'
    }) }}
    {{ form_row(registrationForm.adress) }}
    {{ form_row(registrationForm.codePostal) }}
    {{ form_row(registrationForm.ville) }}
    {{ form_row(registrationForm.pays) }}
    {{ form_row(registrationForm.photoDeProfil) }}
    <div id="numeroDeSiretContainer" style="display: none;">
        {{ form_row(registrationForm.numeroDeSiret) }}
    </div>
    <button type="submit" class="btn">Register</button>
    <button onclick="history.back()">Annuler</button>
    {{ form_end(registrationForm) }}
    <article>
        En créant un compte, vous acceptez les <a href="#">conditions générales de vente</a> de Sell Me.
        Veuillez consulter notre <a href="#">notice Protection de vos Informations Personnelles</a>,
        notre <a href="#">notice Cookies</a> et notre <a href="#">notice Annonces publicitaires basées sur vos centres
            d’intérêt</a>.
    </article>
    <script>
        var natureSelect = document.querySelector('#{{ registrationForm.nature.vars.id }}');
        var numeroDeSiretContainer = document.getElementById("numeroDeSiretContainer");

        natureSelect.addEventListener('change', function (){
            if (this.value === 'professionnel'){
                numeroDeSiretContainer.style.display = 'block';
            } else {
                numeroDeSiretContainer.style.display = 'none';
            }
        });
    </script>

{% endblock %}
